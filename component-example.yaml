apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: example-component
  title: Example Component
  description: A sample component to test the new card UI
  tags:
    - example
    - test-ui
    - typescript
spec:
  type: service
  lifecycle: experimental
  owner: user:guest
  system: default-system